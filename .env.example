# Database Configuration (Aurora MySQL)
# In production, these are injected by AWS SAM from Secrets Manager or CloudFormation outputs
DB_HOST=localhost
DB_PORT=3306
DB_NAME=zero_etl_db
DB_USER=admin
DB_PASSWORD=your_secure_password_here

# External API Configuration
API_ENDPOINT=https://jsonplaceholder.typicode.com

# Application Settings
LOG_LEVEL=INFO

# AWS Configuration (for local testing with boto3)
AWS_DEFAULT_REGION=us-east-1
AWS_PROFILE=default
